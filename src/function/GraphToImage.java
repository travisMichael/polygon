package function;

import domain.Graph;
import domain.Grid;


public class GraphToImage {
    // input is a graph and a grid
    public String apply(Graph graph, Grid grid) {
        // todo
        // implement shoe lace formula
        // grid parameters: x_range, y_range, step_size
        // algorithm to inject points, assign edges to grid boxes
        // grid filling algorithm
        // pixel color area calculator

        // augment graph based on grid params
        // each edge should be referenced from some pixel,

        return "todo";
    }
}